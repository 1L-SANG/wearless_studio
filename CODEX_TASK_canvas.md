# 지시서: 콘티보드 보드 UI 전면 개편 — 스택 아코디언 + 이미지 카드 + 트레이/프레임

## 0. 작업 환경

- **작업 폴더**: /private/tmp/claude-501/-Users-daily-Documents-wearless-studio/bc35ad7e-b054-4fe0-826b-f08c49ff806d/scratchpad/wt-canvas (브랜치 feat/storyboard-canvas-redesign, base=origin/main+#75 병합, 베이스라인 **96 pass·빌드 성공**).
- **디자인 정본**: 같은 폴더의 `mockups/storyboard_redesign_final.html` — 오너 승인 목업. 시각(치수·트랜지션·컬러)은 이 파일을 그대로 재현한다. 열어서 CSS를 참조할 것.
- 검증: `node --test tests/frontend/*.test.mjs` 전부 통과 + `npx vite build` 성공.
- 커밋은 시도하되 권한 오류가 나면 미커밋으로 종료(검토자가 커밋). push 금지.
- **서버(server/**)·저장 계약(blocks 필드 스키마)·자동 배정(generationExamples.js)·인스펙터 내용 변경 금지.**

## 1. 무엇을 바꾸나 (요약)

현행 콘티보드의 세로 로우 카드 + 검정 테두리 세트 밴드 + 좌측 미리보기 레일을 폐기하고:
- 섹션을 **4개 렌더 그룹**(후킹/스타일링/스튜디오/의류 확인)으로 표시
- 각 그룹은 **접힘=카드 뭉치(스택), 펼침=이미지 카드 그리드**의 아코디언(한 번에 한 그룹만 펼침)
- 세트는 **옅은 틴트 트레이**, 한 행 2컷(layoutRow)은 **프레임**으로 표현
- 카드 하단 캡션에 **참조칩**(예시 미니 썸네일+범위)을 포함

## 2. 시드 순서 조정 (유일한 계약 인접 변경 — 신중히)

렌더 그룹과 컷 번호(n/total)가 연속이 되도록 **시드 블록 순서만** 조정한다 (src/lib/api/shapes.js defaultStoryboard — mock은 이 함수를 위임하므로 자동 동승):
- basic: 오프닝, 셀링포인트, 세트A, 세트B, **거울/남성대체**, **회전 세트**, 제품들  (현행: 거울이 회전 뒤 → 앞으로)
- extended: 오프닝, 셀링포인트, 세트A·B·C, **거울/남성대체**, **시퀀스(또는 회전)**, **추가색 호리존 색상블록들**, 제품들(추가색 제품 포함)
- 이렇게 하면 그룹핑(아래 §3)이 곧 연속 구간이 된다: [BENEFIT]=후킹, [styling·mirror]=스타일링, [horizon]=스튜디오, [PRODUCT]=의류 확인.
- 총 컷수·구성 불변(순서만). `isDefaultStoryboardForMode` 왕복과 기존 테스트(순서 의존 단언)를 함께 갱신. 기존 프로젝트 재시드 없음(현행 게이트 그대로).

## 3. 신규 모듈 `src/lib/storyboardRenderGroups.js`

```
export function renderGroups(blocks) → [
  { key:'hooking',  title:'Section 1', label:'후킹',     items:[...] },
  { key:'styling',  title:'Section 2', label:'스타일링', items:[...] },
  { key:'studio',   title:'Section 3', label:'스튜디오', items:[...] },
  { key:'product',  title:'Section 4', label:'의류 확인', items:[...] },
]
```
- 분류: sectionRole BENEFIT→hooking, PRODUCT→product, FIT는 cutType horizon→studio, 그 외(styling·mirror·mine)→styling. 각 item에 전역 순번(1-base)과 블록을 담는다.
- 그룹은 빈 경우 헤더+빈 상태(고스트 타일만)로 렌더.
- 유닛 테스트: 시드(basic·extended, 단색·다색)에서 4그룹 분류·전역 번호 연속성.

## 4. 보드 렌더 (Storyboard.jsx — 인스펙터·저장·자동배정 로직은 그대로 두고 보드 마크업/CSS 교체)

목업의 클래스·트랜지션을 따른다:
1. **아코디언**: 초기 전부 스택. 스택/헤더 클릭 → 해당 그룹 펼침+나머지 접힘, 펼친 그룹 헤더 재클릭=접기. 헤더 전체 히트존 + 회전 셰브론. `grid-template-rows 0fr↔1fr` 트랜지션(목업 CSS).
2. **스택**: 그룹 첫 컷 3장 겹침(회전 오프셋), 호버 부채꼴, 우하단 n컷, 첫 장에 전역 번호 배지.
3. **카드**: 3:4 이미지 + 좌상단 `n/total` 배지 + 호버 액션(복제⧉·삭제✕ — 기존 핸들러 연결) + grip 드래그(기존 dnd 로직 이식 — **같은 그룹 안에서만** 이동 허용, 세트 편입/이탈·트레이 통째 이동 규칙은 현행 유지, 삽입선 색 구분 유지).
4. **캡션**: 방향·샷 라벨(현행 catalogs 라벨), 색상 점(블록 대상 색), 매칭 의류가 있으면 미니칩, **참조칩**(§5). '전부' 스코프에서 블록 방향/샷이 예시와 다르면 해당 값 강조(현행 `.sb-val-changed` 로직 준용).
5. **미배정 카드**: 빗금 배경 + "생성예시를 배정하지 못했어요 · 카드를 열어 다시 시도"(현행 문구·재시도 흐름 유지). **mine 카드**: 업로드 이미지 + '내 사진' 뱃지, 생성 옵션 없음(현행).
6. **세트 트레이**: spaceGroupId 연속 run → 틴트 트레이. 라벨 `📍 {세트명} · n컷`(호리존 세트는 ↻), 우측 "촬영 세트 변경"(기존 인라인 갤러리 흐름 연결)과 ⋯ 메뉴(세트 전체 풀기 — 기존 핸들러). 틴트: normalizePlaceType(storyboardEntryPlacement.js) 결과별 옅은 색 팔레트(카페 라벤더/자연 그린/스튜디오 그레이/기타 라벤더 — 목업 값), 검정 테두리 제거.
7. **프레임**: 같은 layoutRowId 연속 2컷 → 목업 `.frame`(2분할 fbox + f-tag "한 프레임 구성 · 2컷"). 반쪽 클릭=그 블록 선택. **조작**: 현행 코드에 layoutRow 묶기/풀기 UI(레이아웃 칩 등)가 있으면 새 UI에 이식, 없으면 이번 단계는 표시 전용(보고에 명시). 프레임 기본 시드는 만들지 않는다.
8. **사이 추가(addzone)**: 카드 우측 틈 호버 → 세로선+＋ → 클릭 팝오버: 트레이 밖=[＋ 개별 컷][＋ 촬영 세트](기존 두 버튼 흐름·핸들러), 트레이 안=[＋ 이 공간에 컷 추가](기존 흐름). 그룹 끝 고스트 `＋ 컷 추가` 타일=기존 블록 추가 흐름.
9. **좌측 PagePreviewRail 제거**(컴포넌트·CSS·호출부). 상단바는 현행 유지.
10. **인스펙터**: 기존 컴포넌트 그대로(내용 무변경 — 3×3 갤러리는 이미 이 브랜치에 있음). 열림 시 보드 그리드 4열→3열(목업 `body.inspector-open` 방식 준용, 현행 열림 상태 관리 재사용).
11. **반응형**: ≤900px 2열 + 인스펙터 하단 시트(목업 미디어쿼리 수준이면 충분).

## 5. 참조칩

- 데이터: block.exampleId → catalogs.genExamples에서 썸네일, block.refScope(전부/pose/bg).
- 표시: 미니 썸네일(20px)+라벨, 색: 전부=회색·포즈=파랑·배경=초록(목업 값). 호버 팝오버: 예시 크게 + "이 예시의 {연출 전체|포즈만|배경만} 참고".
- 예시 미배정이면 참조칩 생략. mine 카드는 참조칩 없음.

## 6. 하지 말 것

- React 훅 순서 변경 금지(훅은 로딩 early-return 위 — 위반 시 화이트스크린).
- 저장 계약 필드·서버·자동 배정 로직·인스펙터 내부·지문 필드 목록 변경 금지(§2 순서 조정 제외).
- 기존 기능 누락 금지 — 목업 v3.1은 다음을 전수 반영한 검수본이다: 거울컷, 참조칩, 사이 추가, ⋯ 세트 풀기, 미배정 상태, 매칭 미니칩, 공간 스트립(인스펙터), 컷종류 잠금, 다른 예시 보기, 추가 옵션, 값 강조. 구현에서도 전수 유지.
- mockups/·reference/ 수정 금지.

## 7. 검증·보고

- 전 테스트 통과(+신규 그룹핑·순서 테스트) + 빌드. 가능하면 `pnpm dev:mock` 기동 확인(수동 브라우저 검증은 검토자 몫).
- 보고: 변경 파일과 요지 / §2 순서 조정의 파급(수정한 테스트 목록) / 프레임 조작 현행 존재 여부와 처리 / 남긴 리스크.
