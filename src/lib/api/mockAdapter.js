/* =============================================================
   mockAdapter — 기존 mock 레이어 전체를 그대로 위임 (plan §8).
   Phase 7(mock 제거) 전까지 실행 가능한 계약·parity 기준으로 유지.
   ============================================================= */
export { api as mockAdapter } from '@/mock/api.js';
