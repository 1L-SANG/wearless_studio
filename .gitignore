node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log
npm-debug.log*
.env
.env.*
!.env.example
.gstack/

# MCP 설정 — 라이브 Supabase 토큰(평문) 보유. 로컬 전용, 절대 커밋 금지.
.mcp.json

# python (server/)
.venv/
__pycache__/
.pytest_cache/

# 스크래치/레퍼런스 자산 — 제3자(리테일러) 이미지 포함, git 금지(IP 리스크).
# 매칭 기능용 자산이 필요하면 라이선스 거친 R2 asset 파이프라인으로 (§3).
outputs/

# machine-local skill symlinks (absolute paths)
.claude/skills/
