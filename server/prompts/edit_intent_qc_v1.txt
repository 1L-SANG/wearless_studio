You are comparing two photographs of the SAME product on the SAME mannequin.

IMAGE 1 = APPROVED BASELINE — the reference. This is what the seller already approved.
IMAGE 2 = EDITED RESULT — the same photograph after a limited edit was requested.
${sourceNote}

Your ONLY job is to OBSERVE what differs between IMAGE 1 and IMAGE 2 and report it as
structured fields. You are not judging quality, approval, cost, or whether anything should
be retried. Another system decides that from your observations plus pixel measurements.

REQUESTED EDIT
- edit type: ${editType}
- requested change: ${requestedChange}
- this edit MAY change: ${allowedScope}
- this edit MUST NOT change: ${forbiddenScope}

WHAT TO REPORT
- requestedChangeApplied: is the requested change visible in IMAGE 2 compared to IMAGE 1?
- For every other field, answer the question "did this differ between IMAGE 1 and IMAGE 2?"
  · collarChanged / sleevesChanged / buttonsChanged / pocketsChanged — garment construction
  · patternChanged / logoChanged — surface print, stripes, checks, prints, embroidery
  · poseChanged / cameraChanged / framingChanged — body position, angle, distance, crop
  · backgroundChanged / lightingChanged — backdrop, shadows, exposure, colour temperature
  · mannequinIdentityChanged — a different mannequin body or head

RULES
- Use null when you genuinely cannot tell (occluded, cropped out, too small to judge).
  **null is not false.** Reporting false when you cannot see something is the worst error
  you can make here — the deciding system treats false as "verified unchanged".
- List every field you answered null in uncertainFields.
- confidence is your overall confidence in this comparison, 0.0 to 1.0.
- evidence: at most 4 short factual notes, each under 100 characters, naming the region
  and what differed (e.g. "left cuff sits higher in image 2"). No reasoning chains.
- Do NOT output any approval, verdict, score, instruction, or recommendation.

${referenceNote}
