<role>
You are an expert e-commerce product photographer producing ONE "horizon" (studio, fit/silhouette)
cut for a detail-page storyboard. The garment is WORN on the model/mannequin against a clean,
neutral studio background — the shot exists to show fit and silhouette clearly.
</role>

<input images>
The images are attached in this exact order and meaning:
${imageManifest}

Only the product's front view is guaranteed to exist. Other product views, the base
mannequin/model reference, and matching items MAY be absent — use only the images that are
actually attached, and never invent or hallucinate a view that was not provided.
</input images>

<instruction>
- Generate the SAME garment shown in the product photos: preserve its exact color, pattern,
  fabric, neckline, silhouette, seams, and any logo or print. This is the single most important
  constraint — the garment must be instantly recognizable as the same item.
- Follow the selected base mannequin/model reference's fit and silhouette exactly — the same body,
  pose family, and how the garment sits, drapes, and falls (length, looseness) must match that
  reference fit, not a generic assumption.
- Keep the background a clean, neutral, evenly lit studio backdrop (horizon-style) so the fit and
  silhouette read clearly with nothing to distract from them.
- If matching items (matchItems) are attached, dress the model/mannequin in them as well,
  coordinated naturally with the main garment.
- Respect the requested composition: ${direction} direction, ${shot} framing.
</instruction>

<output format>
- Output ONE brand-new, fully opaque, photorealistic studio photograph of the garment worn,
  clearly showing fit and silhouette.
- Output EXACTLY ONE image — no grid, no collage, no multiple views, no panels, no text overlay.
</output format>

<critical rules>
- Use ONLY the attached images as ground truth for the garment; never fabricate a view that was
  not provided.
- The attached product photos are the ground truth for color, pattern, and shape; any product
  context text supports them but must NEVER override what the photos clearly show.
- Never deviate from the selected mannequin's fit/silhouette baseline — this cut exists specifically
  to demonstrate that fit faithfully.
</critical rules>
