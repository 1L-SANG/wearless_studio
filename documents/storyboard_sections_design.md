# 콘티보드 섹션 개편 — 설계 기록 (2026-07-10)

Claude(Fable)–Codex(GPT-5.6-sol ultra) 교차 토론으로 확정, 같은 날 1차 구현 완료.
사용자(창업자) 바인딩 결정이 최상위, 그 아래 토론 수렴 사항.

## 1. 바인딩 결정 (사용자)

1. 콘티보드는 **항상 블록 카드의 세로 스택** — 상세페이지 세로 순서의 미니어처. 썸네일 가로 나열식 '이미지 뷰어' UI 금지.
2. **섹션 depth=1** — '같은 장소 시리즈'도 하나의 섹션. 섹션 안에 하위 묶음 없음.
3. 섹션 종류·순서 taxonomy는 차기 ultracode 수집(상세페이지 구조도)으로 확정. 현행은 컷 어휘 기반 잠정(인트로/같은 장소 시리즈/스타일링/호리존/거울샷/제품).
4. 드래그 자유 이동은 유지, 추가 효과는 **섹션 상단 배경 회색 변화 수준만**.
5. 접기/펼치기 채택 — 접으면 스택(덱) 형태.
6. 샷 버튼 자동화(숨김)는 기각 — 샷 타일 상시 노출 유지.

## 2. 토론 수렴 (Codex 반박 → 수용된 것)

- **반폭 카드 기각**: 570px 보드에서 2단을 카드 축소로 미러링하지 않는다. 레이아웃 칩은 헤더에, 카드는 항상 풀폭. '2단'은 추후 `1행 왼쪽/오른쪽` 슬롯 배지 방식으로(백로그).
- **레이아웃 칩은 배선된 것만 노출**: v1 = 세로 1열 + 컬러 비교(생성기 배선 포함). 장식 칩 금지.
- **섹션 앵커 ≠ 컷 예시**: 같은 장소 시리즈의 배경 기준은 섹션 앵커(추후 `anchorExampleId`), 시리즈 안 컷의 예시는 '포즈 예시'로 강등(범위 고정). 
- **refScope v1 = 전부|포즈만**: '배경만'은 공간 앵커·배경 변형 기능과 역할이 겹쳐 보류.
- 고지 문구(확정): 포즈만 = "포즈만 참고해요. 배경에 맞지 않으면 자세와 구도가 자연스럽게 조정될 수 있어요."
- 정보 rows(방향·샷·색) 상시 노출 — hover 숨김 폐기(승인 목업과 정합).
- 회색 효과는 토큰(--bg-2)만 사용. 이동은 출발·대상 섹션 모두 '직접 구성' 표시. 제자리 드롭 무시.
- 포즈를 직접 고르면 포즈-범위 예시는 효력이 없으므로 자동 정리.
- (기각) Codex의 '접기 v1 보류' 권고 — 사용자 지시 우선으로 접기 구현함.

## 3. 구현 내역 (1차, 빌드·화면 스모크 통과)

- `src/lib/sections.js` (신규): 섹션 = 블록 필드(sectionId/Title/Layout/Custom)의 연속 run. 저장 계약(blocks:list) 무변경. `ensureSections`(구버전 저장분 마이그레이션), `deriveSections`, `adoptSection`(소속 채택+공간 계약 해제+양쪽 직접구성), `patchSection`. **경계 드롭 중의성 해소**: 드롭라인·덱은 특정 섹션 몸통 안에 렌더되므로 드롭 시 그 섹션 id를 명시 전달(`adoptSection(_,_,targetSid)`) — 위치 추론이 아니라 드롭 대상이 정답. 화살표 이동 등 타깃 불명일 때만 "이웃 중 자기 섹션 우선" 폴백.
- `src/mock/db.js`: 시드 콘티 섹션 부여, `buildEditorBlocksFromStoryboard`에 컬러 비교 레이아웃 배선(같은 섹션 연속 컷 3열 청크).
- `src/features/storyboard/Storyboard.jsx`: 섹션 밴드 렌더(헤더: 접기 셰브론·제목·같은 장소 배지·N컷·직접 구성·레이아웃 칩·＋컷 추가), 접기 덱(부채꼴 3장+컷수), 드래그 hot/edited 회색, 삽입·업로드·이동 전부 adoptSection 경유(스냅샷은 소속 부여 후 — '원래대로' 섹션 유실 방지), 섹션 ＋컷은 다수 컷종류·미보유 샷 프리필+시리즈 공간 계약 상속, MoodGuide에 refScope(전부|포즈만)+시리즈 내 '포즈 예시' 강등+고지, 새 컷 빈 상태에 추가 위치·크레딧·'이 블록 취소'.
- `src/styles/features.css`: 섹션 밴드/덱/칩/스와치 레일/scope 칩/newmeta 스타일(전부 기존 토큰).

## 3.5 2차 반영 (2026-07-10, 사용자 지시 + Codex 토론)

- **포즈 편집 폐기** — 인스펙터 편집 패널은 '매칭 의류 편집'만. 포즈는 생성예시(레퍼런스 범위)가 담당.
- **레퍼런스 범위 = 예시 셀 호버 오버레이** — 이미지 어두워지며 '레퍼런스 범위' + [전부][포즈만] 선택(시리즈 안은 포즈만 고정, 제품·비정면 제외), 선택 셀에 범위 배지, 재클릭 해제.
- **레이아웃 UI v1 출시** — 연속 AI 컷 2개 이상 섹션에 '상세페이지 배치' 칩 상시 노출([세로 1열][2단], 컬러 비교는 자격제·자동 강등), 픽토그램 포함. **2단 mock 생성기 배선**(rowSizeFor 공유 계획, 홀수 마지막 풀폭), 카드 썸네일에 슬롯 배지(N행 왼쪽/오른쪽/전체, 컬러 비교=색점+색명). http는 M-02까지 전체 숨김.
- 교훈: 신규 훅은 로딩 early-return 위에 (훅 개수 불변 — 위반 시 화이트스크린, 스모크로 검출·수정).

## 4. 백로그 (다음 반복)

1. ~~2단 레이아웃~~ → mock 배선 완료(§3.5). 잔여: M-02 서버 조립 소비(7번과 통합).
2. 같은 장소 시리즈 **앵커 UI** — 섹션 요약 박스(기준 예시 썸네일·기준 바꾸기), `anchorExampleId` 블록 미러 필드.
3. 새 컷 **풀 컴포저** — 미커밋 드래프트(컨텍스트 헤더→컷종류→샷→예시 6칸→기본값 row→추가 CTA), 경계 삽입=새 섹션 + 인접 공간 제안("공간 A에 넣을까요?"), '같은 장소 세트 추가' 별도 진입.
4. 시리즈 '가입' 명시 액션(현재 이탈만 자동, 가입은 섹션 ＋컷으로만).
5. 섹션 taxonomy·레이아웃 프리셋 — 차기 ultracode 수집(구조도, ANCHOR_REGEN_PLAN §0.5) 후 확정.
6. ~~생성 계약 — `refScope`의 서버 소비~~ → **완료(2026-07-10)**: `cut_generator.normalize_spec`이 refScope('all'|'pose', 그 외 'all' 클램프)를 정규화하고, 'pose'면 예시 뉘앙스(EXNUANCE)에 `[[REFSCOPE:pose]]` 가드(배경·장소 미전이)를 덧붙인다. **불변식**: 같은 장소 세트 안 예시(spaceGroupId+exampleId)는 레거시 저장분·'all' 값도 서버가 'pose'로 강제(배경은 [[SPACE]] 담당). pytest 3건 추가(241 pass).
7. 생성 계약(잔여) — `sectionLayout: colorCompare`의 **서버 조립(M-02 page_assembler)** 소비. mock 생성기는 배선됨; **http 모드에서는 칩 자체를 숨김**("배선된 칩만" 규칙, 2026-07-10 최종 리뷰 반영). 서버 조립 구현 시 게이트 해제.
8. (최종 리뷰 잔여, Medium-low↓) ⓐ 컬러 비교 자격 상실(색 삭제·변경) 시 stack 자동 강등, ⓑ 새 컷 취소 시 '직접 구성' 마킹 원복(커밋 시점 마킹으로 이연), ⓒ refScope '전부' 문구가 v0 예시 한계(구도 프리셋 수준, 예시 이미지 미전달)를 과장 — **예시 자산 실배선은 생성예시 앵커 재생성 트랙(ANCHOR_REGEN_PLAN)이 선행**이라 그때 함께 해소.

## 5. 2026-07-11 Codex 리뷰 위생 수정 (커밋 전 워킹트리)

Codex working-tree 리뷰 4건(P2) 전부 실확인 후 수정 — 공통 원인은 "구조 변경 경로 중 일부만 normalizeBoard/계약 상속을 태움":

1. **복제(duplicate) 후 normalizeBoard 누락** → 컷 수 변화에 레이아웃 스테일(예: 2컷 twoColumn에 복제 → 활성 칩 없음) — 삽입·이동과 동일 위생 적용.
2. **삭제(remove) 후 normalizeBoard 누락** → 동일 수정( undo 경로도 adoptSection 뒤 normalizeBoard).
3. **같은 공간 섹션에 ＋컷 추가 시 spaceGroupId 미상속** → deriveSections가 시리즈 해제, SPACE 연속성·포즈 범위 계약 소실. adoptSection의 "가입 자동 금지"는 이동용 규칙 — **명시적 추가 = 가입**으로 상속(그룹+variation). (백로그 4번의 절반이 이걸로 해소 — '섹션 ＋컷' 경로는 가입 동작 확보)
4. **에디터 AIPanel의 MoodGuide 범위 오버레이** — onRefScopeChange 미전달 소비처에선 '포즈만' 선택이 저장·전송 안 되는 빈 클릭 → 핸들러 없으면 범위 UI 숨김. **후속(백로그)**: 에디터 새 컷 요청에 refScope 실배선(6번의 에디터 경로판).

검증: vite build ✓. (콘티 서버 계약 무변경 — 전부 프론트 보드 위생)

5. **(stop-리뷰 후속) 삭제 실행취소의 레이아웃 복원** — 2번 수정으로 삭제가 레이아웃을 적극 강등하게 되면서, undo 의 "재삽입+재정규화"만으론 원래 레이아웃·행이 안 돌아오는 손실이 노출됨 → **삭제 전 보드 통짜 스냅샷**을 undo 정본으로(삭제 직후 상태 identity 가 그대로일 때만, addBlock 취소와 동일 패턴 — 이후 조작 시 자동 무효화되어 기존 폴백 유지).

6. **(stop-리뷰 2·3차 후속) 자동 강등 effect 와 undo 스냅샷 판정** — colorCompare 자격상실 자동 강등 effect(`patchSection(stack)`)가 삭제 직후 스스로 발화하면 배열 identity 가 바뀌어 5번의 통짜 복원이 영영 폴백으로 빠짐. 1차 시도(필드 관용 비교: sectionLayout·layoutRowId 무시)는 **사용자의 stack 칩 변경과 자동 강등의 필드 서명이 동일**해 행위자 구분 불가(3차 리뷰 지적) → 최종: effect 가 자기가 만든 배열의 **계보를 WeakMap(autoDemoteTrail)에 기록**하고, undo 는 현재 보드가 postDelete 계보 위에 있을 때만 스냅샷 복원. 사용자 조작은 계보 밖이라 자동 폴백 — 필드가 아니라 행위자로 판정.

## 7. 레퍼런스 범위 3종 확정 (2026-07-12, 실생성 스파이크로 검증)

**결론: refScope = 전부(all) | 배경만(bg) | 포즈만(pose).** 각 범위는 **전용 자산**을 첨부한다 — 텍스트 지시만으로는 픽셀 유입(포즈·소품·의류)을 막지 못한다는 것이 두 차례 실험으로 확정됐다.

| 범위 | 첨부 자산 | 스파이크 결과 |
|---|---|---|
| 전부 | 원본 예시 | 기존 동작 |
| 배경만 | **빈 무대 플레이트**(인물 제거 + 여백 확보 재구성) | 원본 첨부 시 **예시의 의류·신발이 전이**(계약 위반) + 크롭 유출 → 플레이트로 해결 |
| 포즈만 | **누끼**(배경 제거, 인물만) | 원본 첨부 시 예시의 의자·가방 등 **소품·무드 전이** → 누끼로 해결. 걷기 포즈 충실도도 누끼가 우수 |

- **플레이트 생성 규칙(중요)**: 단순 인물 제거로는 "인물이 화면을 채운 예시"에서 구도가 무너진다(머리 잘림). 플레이트는 **"전신이 들어갈 여백 있는 빈 무대"로 재구성**해야 하며, 컷 프롬프트에 **FRAMING OVERRIDE**(요청 샷 > 예시 크롭) 가드를 함께 넣는다.
- **비용**: 누끼·플레이트는 앵커 확정 시 예시당 사전 1회 생성(총 2콜) — 셀러 생성 시점 추가 비용 0.
- **계약**: 제품컷은 배경만/포즈만이 성립하지 않아 '전부'로 강등. 같은 장소 시리즈는 배경이 세트 기준이므로 '포즈만' 강제(기존).
- **구현**: `[[REFSCOPE:bg]]` 프롬프트 섹션, 매니페스트 역할(EMPTY SET plate 명시), 자산 레지스트리 3-variant(`{all,pose,bg}`), 인스펙터 호버 3칩, 카드 '배경'/'포즈' 미니. 서버 288 pass.
- **재사용 여지**: 빈 무대 플레이트는 보류했던 '같은 장소 세트'의 배경 기준(플레이트 방식, 결정 3-B)과 동일 기술 — 그 트랙 재개 시 재사용.
- 실험 산출물: `mockups/포즈만-실험.html`, `mockups/배경만-실험.html` (스크립트: `spike/posescope/`).
